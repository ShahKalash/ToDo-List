<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://kit.fontawesome.com/1362334b71.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans&family=Libre+Franklin:wght@200&display=swap"
        rel="stylesheet" />
    <link rel="stylesheet" href="css/styles.css" />
    <title>To-Do List
    </title>
</head>

<body>
    <div class="container">
        <header class="text-center text-light my-4">
            <h1 class="mb-4">Todo List : <%=title%>
            </h1>
            <form class="search" action="/search" method="post">
                <input class="form-control m-auto" name="search" placeholder="search todos" autocomplete="off" />
                <input type="hidden" name="title" value="<%=title%>">
            </form>
        </header>

        <ul class="list-group todos mx-auto text-light delete" id="myUL">
            <% data.forEach(element=> { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <%= element.name %>
                        <form action="/delete" method="post">
                            <button type="submit" class="btn" value="<%=element._id%>" name="tobedeleted"><i
                                    class="fa fa-trash"></i>
                            </button>
                            <input type="hidden" value="<%=title%>" name="title">
                        </form>
                </li>
                <% }); %>
        </ul>

        <form class="add text-center my-4" action="/" method="post">
            <label class="text-light">Add a new todo...</label>
            <input class="form-control m-auto" type="text" name="item" autocomplete="off" />
            <br />
            <div class="text-center">
                <button type="submit" value="<%=title%>" name="title" class="btn btn-light">Add</button>
            </div>
        </form>
    </div>

    <script>
        var list = document.querySelector('ul');
        list.addEventListener('click', function (ev) {
            if (ev.target.tagName === 'LI') {
                ev.target.classList.toggle('checked');
            }
        }, false);

    </script>
</body>

</html>